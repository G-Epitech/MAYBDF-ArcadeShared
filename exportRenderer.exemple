#include "types/LibraryType.hpp"

extern "C" shared::types::Renderer arcadeLibGetType()
{
    return shared::types::LibraryType::RENDERER;
}

extern "C" std::unique_ptr<shared::graphic::IRenderer> arcadeLibGetRenderer()
{
    return std::make_unique<RendererClass>(...);
}

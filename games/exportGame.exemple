#include "types/LibraryType.hpp"

extern "C" shared::types::Game arcadeLibGetType()
{
    return shared::types::LibraryType::GAME;
}

extern "C" std::unique_ptr<shared::games::IGame> arcadeLibGetGame(void)
{
    return std::make_unique<GameClass>(...)
}
